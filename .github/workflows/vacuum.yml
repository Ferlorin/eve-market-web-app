name: Database Vacuum

on:
  workflow_dispatch:

jobs:
  vacuum:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: webapp/package-lock.json

      - name: Install dependencies
        working-directory: webapp
        run: npm ci --prefer-offline --no-audit

      - name: Generate Prisma client
        working-directory: webapp
        run: npx prisma generate

      - name: Run VACUUM ANALYZE on market_orders
        working-directory: webapp
        run: npx tsx -e "import { prisma } from './src/lib/db.ts'; await prisma.\$executeRawUnsafe('VACUUM ANALYZE market_orders'); console.log('✓ VACUUM ANALYZE completed successfully'); await prisma.\$disconnect();"
        env:
          NEON_DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}
